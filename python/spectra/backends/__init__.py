"""Spectra GUI backends for embedding plots in application frameworks.

Available backends:
    - ``spectra.backends.backend_qtagg`` — Qt5/Qt6 (PyQt5, PySide2, PyQt6, PySide6)

Usage (matplotlib-compatible style)::

    from spectra.backends.backend_qtagg import FigureCanvasSpectra, NavigationToolbarSpectra

Or use the convenience widget::

    from spectra.backends.backend_qtagg import SpectraWidget
"""
